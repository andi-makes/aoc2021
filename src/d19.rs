use super::{Runnable, Day};

type Data = Vec<i32>;
impl Runnable<Data> for Day<19> {
    fn init() -> (Self, Data) {
        let v = vec![0];
        (Self {}, v)
    }
    fn one(&self, data: &mut Data) {
        
    }
    fn two(&self, data: &mut Data) {
        
    }
}

